// engine/constants.ts
export const DEFAULT_SESSION_DURATION_SEC = 4 * 60 * 60; // 4 jam
export const DEBUG_DEFAULT_DURATION_SEC = 12; // untuk testing cepat
export const DEFAULT_BASE_RATE = 0.5; // IGT/s base
export const DEFAULT_DECAY_PER_CLAIM = 5; // % health lost per claim
export const DEFAULT_REPAIR_PCT = 1; // 1% (used as percent, convert to decimal when computing)
