// engine/constants.ts
export const DEFAULT_SESSION_DURATION_SEC = 4 * 60 * 60; // 4 jam (sec)
export const DEBUG_DEFAULT_DURATION_SEC = 12; // buat testing cepat
export const DEFAULT_BASE_RATE = 0.5; // IGT/s default per mesin (tier base)
export const DEFAULT_DECAY_PER_CLAIM = 5; // % health hilang tiap claim
export const DEFAULT_REPAIR_PCT = 1; // biaya repair = X% dari total session gross (configurable)
