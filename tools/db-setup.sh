#!/usr/bin/env bash
# tools/db-setup.sh
# Usage: node tools/seed-demo.js OR run this script to show help.
echo "This helper creates indexes and can run seed script (not executed automatically)."
echo "Run: node tools/seed-demo.js --create-indexes  OR  node tools/seed-demo.js --seed-demo"
